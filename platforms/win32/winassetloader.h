#ifndef ASSET_LOADER
#define ASSET_LOADER

/*#include "x2d/assetloader.h"

class AssetLoader : public xdAssetLoader
{
public:
	AssetLoader(const string &assetDir);
	~AssetLoader();

	int loadAsset(const char* assetPath, const char** data, long* len = 0);
	int transformFilePath(string &assetPath);
	void loadPlugins();

private:
	string m_assetDir;
};*/

#endif  // ASSET_LOADER